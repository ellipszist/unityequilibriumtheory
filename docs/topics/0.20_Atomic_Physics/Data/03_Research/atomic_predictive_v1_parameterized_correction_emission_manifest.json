{
  "schema_version": "1.0",
  "topic": "0.20_Atomic_Physics",
  "manifest_id": "AT20-PREDICTIVE-V1-PARAMETERIZED-CORRECTION-EMISSION-MANIFEST",
  "purpose": "Define the contract for parameterized delta_uet_or_ci emission in the first fixed-CI/correlated kernel lane before any accepted correction operator exists.",
  "status": "CORRECTION_EMISSION_CONTRACT_READY_IMPLEMENTATION_MISSING",
  "selected_lane_id": "fixed_parameter_ci_or_correlated_two_electron_v1",
  "selected_operator_class": "fixed_parameter_ci_or_correlated_two_electron_correction",
  "target_module": {
    "path": "Code/03_Research/Research_Atomic_Operator_V1.py",
    "required_entrypoint": "get_parameterized_correction_emission_contract"
  },
  "required_contract_fields": [
    "emission_status",
    "upstream_evaluation_dependency_id",
    "emitted_operator_id",
    "required_inputs",
    "required_outputs",
    "blocked_claims"
  ],
  "required_inputs": [
    "evaluated Hamiltonian or effective-operator outputs",
    "locked parameter set or explicit review-only placeholder policy",
    "row-level observable targets and baseline comparator context",
    "holdout-excluded emission policy",
    "machine-readable uncertainty handoff status"
  ],
  "required_outputs": [
    "accepted-or-diagnostic delta_uet_or_ci row values",
    "claim-use classification for every emitted row",
    "row-level provenance linking emission to source inputs and operator state"
  ],
  "required_checks": [
    {
      "check_id": "EMIT-01",
      "requirement": "The target module exposes the parameterized correction-emission contract entrypoint."
    },
    {
      "check_id": "EMIT-02",
      "requirement": "The contract explicitly depends on the Hamiltonian/effective-operator scaffold instead of skipping evaluation provenance."
    },
    {
      "check_id": "EMIT-03",
      "requirement": "The contract explicitly names delta_uet_or_ci as the emitted operator target."
    },
    {
      "check_id": "EMIT-04",
      "requirement": "The contract lists required inputs and outputs explicitly rather than leaving emission expectations implicit."
    },
    {
      "check_id": "EMIT-05",
      "requirement": "The contract still states that accepted correction emission is not implemented, so diagnostic rows cannot be upgraded into accepted delta_uet_or_ci output."
    }
  ],
  "claim_boundary": "This manifest defines the parameterized delta_uet_or_ci emission contract only. It does not emit accepted correction rows, validate a correction operator, or close row-level uncertainty provenance."
}
