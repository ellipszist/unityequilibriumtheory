# 📐 ANALYSIS: SAW Acoustic Field & Gradient Trapping (Topic 0.34)

This document resolves the "Field Ambiguity" in Topic 0.34 by formally defining the Surface Acoustic Wave (SAW) actuator field and its interaction with matter.

---

## 1. Field Definition: $I$ vs $C$

To satisfy the user's specific request for a process-driven model, we define the variables as follows:

- **$I(x,y)$ (The Information/Actuator Field):** This is the **SAW Potential Field**. It represents the spatial distribution of acoustic pressure nodes and antinodes.
  - $I$ is the **Input (Command)**.
- **$C(x,y)$ (The Matter Field):** This is the **Matter Concentration** (Graphene/Perovskite).
  - $C$ is the **Output (Result)**.

## 2. Acoustic Radiation Force ($F_{ARF}$)

Instead of "Stickiness," the atoms are moved by the **Gradient of the Field**. 

The force on an atom of volume $V$ in a SAW field with potential $\Phi$ (mapped to $I$) is:
$F_{ARF} = - \nabla U_{rad} = - \nabla \left[ V \left( \frac{1}{2} \kappa_{m} \langle p^2 \rangle - \frac{3}{4} \rho_{m} \langle v^2 \rangle \right) \right]$

In our normalized UET form:
$\dot{C}_{drift} = - \beta \nabla \cdot (C \nabla I)$

---

## 3. The Math Paradox: Resolving "Who Drives Whom"

The user correctly identified that the AI was confused between **Emergence (A2)** and **Control (A6)**.

| Logic | UET Axiom | 0.34 Application |
| :--- | :--- | :--- |
| **Logic of Emergence** | Axiom 2 ($\beta CI$) | Used for analyzing how pattern density *reflects* info. |
| **Logic of Purpose** | Axiom 6 (Learning/NEA) | **The Manufacturing Mode.** $I$ is the *Command* that drives $C$ to its target. |

**The Selectivity Mechanism**: By using a **Drift Term** ($\nabla \cdot (C \nabla I)$), we physically sweep atoms from high-pressure zones (antinodes) to low-pressure nodes. This ensures that even if there is constant precursor flux, matter only aggregates at the SAW nodes, resolving the **Selectivity Crisis**.

---

## 4. Hardware Realization (The SAW Grid)
The $I$ field is generated by **Interdigital Transducers (IDTs)** on the substrate edges. By modulating the frequency and phase of these GHz IDTs, we change the position of the nodes ($I_{min}$), effectively moving the "Mask" in software.
