# Baseline Comparison

- Comparator implementations live under `Code/04_Competitor/`
- The baseline question for this topic is whether the repository's UET configuration is
  competitive with documented comparator scripts on the same local dataset package
- Claims should be phrased as `internal benchmark comparison` unless an external benchmark
  protocol is documented and rerun independently
