# Data Manifest

Current data reality status: "real source referenced"

| Item | Local path | Source | Provenance status |
|:--|:--|:--|:--|
| download_data.py | `Data/03_Research/download_data.py` | Topic-local working copy or generated benchmark input | Real source is referenced, but manifest normalization is still in progress. |
| higgs_coupling_data.json | `Data/03_Research/higgs_coupling_data.json` | Topic-local working copy or generated benchmark input | Real source is referenced, but manifest normalization is still in progress. |
| higgs_mass_combined.json | `Data/03_Research/higgs_mass_combined.json` | Topic-local working copy or generated benchmark input | Real source is referenced, but manifest normalization is still in progress. |
| lepton_data.json | `Data/03_Research/lepton_data.json` | Topic-local working copy or generated benchmark input | Real source is referenced, but manifest normalization is still in progress. |

Repository note:

- This manifest was created during the repo standards pass and should be tightened further in a later provenance-normalization wave.
- Until upstream URLs, DOIs, preprocessing notes, and hashes are frozen, treat the dataset package as an internal working copy rather than an archival release.
