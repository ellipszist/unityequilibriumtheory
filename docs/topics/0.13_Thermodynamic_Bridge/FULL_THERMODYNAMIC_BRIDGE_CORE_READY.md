# Topic 0.13 Full Thermodynamic Bridge Core-Ready Gate

This note is the human-readable companion to:

`Result/artifacts/topic13_full_thermodynamic_bridge_core_ready_gate.json`

The gate reports major-result progress. It does not promote Topic 0.13 or the
global UET claim.

MAJOR_RESULT_CLOSURE: `PARTIAL`

WHAT_IS_ACTUALLY_CLOSED: The standard normalized TTG operator, normalized Phi
operator, frozen-C compact-support control, and constraint-only Landauer and
standard thermodynamic identities are explicit and artifact-linked.

WHAT_REMAINS_OPEN: The full-candidate causal branch, a formal conserved-C no-go
or explicit regularization, an independent `alpha_Phi_K`, source-normalized
numeric rows, the non-circular UET bridge and `beta`, EOS/transport/SK-KMS,
entropy-current/dissipative balance, and dimensional heat/entropy maps.

DEPENDENCY_UNLOCKED: Only normalized/internal thermal control inheritance is
available. Gravity/GR remains blocked until Full Topic 13 and Core curved 3+1
dependencies are ready.

STATUS: `BLOCKED_OPEN_T13_FULL_BRIDGE`

WHAT_CHANGED: Added a deterministic full-bridge gate that composes causal,
source, calibration, bridge, EOS/transport/entropy, dimensional-map, and
holdout evidence without changing the existing threshold or consuming Xie
2026.

EQUATION_OR_MAPPING:

```text
y_TTG = Delta_Tq(t) / Delta_Tq(0)
y_TTG^UET = Delta_Phi(t) / Delta_Phi(0)
Delta_Tq = alpha_Phi_K * Delta_Phi
```

VERIFICATION: The selected frozen-C control has zero measured pre-arrival
leakage, but it is not the full coupled candidate. The full candidate remains
above the locked `1e-6` threshold. The holdout-integrity gate passes because
Xie 2026 remains metadata-only and numeric fitting is disabled.

CONTROLLING_BLOCKER: `formal_conserved_C_no_go_or_explicit_regularization_missing`,
`ttg_numeric_source_package_is_provisional`,
`alpha_Phi_K_independent_calibration_missing`,
`non_circular_uet_bridge_and_beta_derivation_missing`,
`eos_transport_kms_entropy_completion_missing`, and
`dimensional_phi_to_thermal_observable_map_missing`.

NEXT_ACTION: Close the causal branch/no-go record first, then obtain the
independent calibration and source package before attempting full bridge,
EOS, transport, KMS, or entropy promotion.

CLAIM_BOUNDARY: `Phi` is not temperature, heat flux, entropy, or a particle;
`R_gen` remains a derived history trace without backreaction; no prediction,
external validation, or global theory closure is claimed.
