# Topic 13 Ding PBTE Numeric-Input Availability Wave

MAJOR_RESULT_CLOSURE:
`T13_DING_PBTE_OA_NUMERIC_INPUT_NO_GO` is `CLOSED_FOR_LANE`.

WHAT_IS_ACTUALLY_CLOSED:
The complete captured `PMC8755757.1/` object prefix is archived and audited. It contains 11 objects: four article figures, three PDF supplementary/reporting files, and four article representations/metadata files. It contains no force constants, Phonopy/ShengBTE input, scattering matrix, mode-resolved `c_mu(T)`, or numeric `C_src(T)` payload. The publication routes supporting data to a corresponding-author request.

WHAT_REMAINS_OPEN:
Author-held data may exist, and an independent open graphite phonon reproduction package can still be built. Numeric `C_src(T)`, its uncertainty/convergence, `e0`, base `Phi -> Delta_u_ph`, independent `alpha_Phi_K`, and Full Topic 13 remain open.

DEPENDENCY_UNLOCKED:
The source-acquisition decision is now explicit: request the published calculation payload or build a separately labeled independent reproduction. No Core, Gravity, transport, or external-claim dependency is unlocked.

STATUS:
`PASS_SCOPED_OA_NUMERIC_INPUT_AVAILABILITY_NO_GO`; Full Topic 13 remains `PARTIAL / BLOCKED_OPEN_T13_FULL_BRIDGE`.

WHAT_CHANGED:
Archived the PMC OA API record, complete S3 prefix inventory, object metadata, and full text with hashes. Added an inventory verifier, gate/register integration, acquisition checklist, tests, and a durable no-go boundary.

EQUATION_OR_MAPPING:
The required source quantity remains:

```text
C_src(T) = sum_mu c_mu(T)
Delta_Tq = Delta_u_ph / C_src(T)
```

The numerical `c_mu(T)` or equivalent reproducible phonon payload is not present in the captured official OA distribution. No value is inferred from the normalized TTG curve.

VERIFICATION:
The verifier checks all archived hashes, OA identity/license/retraction status, complete non-truncated 11-object prefix, seven media payloads, file types, absence of reproduction-payload candidates, author-request statement, published VASP/optB88/supercell/q-mesh specifications, missing-input checklist, and Xie 2026 non-access.

CONTROLLING_BLOCKER:
`dimensional_phi_energy_anchor_or_independent_alpha_calibration_missing`. The immediate source-acquisition controller is `ding_pbte_author_data_or_independent_reproduction_package_missing`.

NEXT_ACTION:
Prepare a source-specific request for the relaxed structure, force constants, ShengBTE control/scattering outputs, and `c_mu(T)`, or source-lock an independent open graphite phonon package and label it independent reproduction.

CLAIM_BOUNDARY:
This is a no-go only for the captured official PMC OA route. It is not a proof that the data do not exist elsewhere, a physics no-go, numeric calibration, external validation, Full Topic 13 closure, or global UET closure.
