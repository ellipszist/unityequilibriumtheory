# Limitations

- The root baseline comparison is present, but numeric acceptance boundaries are still provisional until a saved artifact is generated and reviewed.
- Current data posture is source-referenced but still below a fully normalized archival dataset package.
- Berut 2012 and exact SI/CODATA source records are now pinned under `docs/data/external/...`, and the current Berut summary row now has one exact preview-level `Figure 3` locator attached under the selected figure-level policy, but it still remains a topic-derived summary row rather than a closed numeric-point or row-level source package.
- The data package still contains manual literature summaries, so the topic cannot yet claim fully standardized data provenance.
- `source_evidence_intake_stub.json` and `source_evidence_readiness_matrix.json` are process controls, not evidence by themselves; an empty or pending-ready gate cannot be cited as provenance closure.
- The source-evidence readiness matrix is now more informative than before, but `ready_for_source_review` still does not mean `source-lock closed`. It means enough provenance fields are present for a human source-review pass.
- `uncertainty_preprocessing_manifest.json` now narrows the next uncertainty wave with concrete Berut, Jun, GW150914, M87*, and Sgr A* rows, but it is still only a preprocessing scaffold.
- `uncertainty_propagation_summary.json` now adds propagated intervals for `5/5` tracked rows, but this is still incomplete: the Berut interval is based on a topic-summary row, the Jun interval is still only tied to a pinned source-facing summary rather than a tightly archived row/file package, and gravity-context rows use only mass uncertainty plus a provisional `G` proxy while still excluding systematic terms.
- `ROW_CLOSURE_MATRIX.md` and `row_closure_matrix.json` now narrow the blocker chain row-by-row, but they do not close it: Berut still needs row-level provenance, Jun still needs a source-backed uncertainty row, Peterson still has a composite source-reference conflict, and gravity-adjacent rows still stop short of systematic astrophysical-term closure and systematic-term closure.
- `LANDAUER_ROW_CONTRACT.md` and `landauer_row_contract.json` now make the Berut-versus-Jun split more explicit, but they do not close either row: Berut still lacks one authoritative upstream numeric surface or declared policy path plus row mapping, and Jun still lacks both a closed runtime-to-source quantity mapping and a source-backed uncertainty value.
- `BERUT_2012_PROVENANCE_GAP.md` and `berut_2012_provenance_gap.json` now make the `Berut` blocker more explicit, but they do not close it: the topic now has one exact `Figure 3` preview locator, yet it still lacks one numeric point or stronger upstream numeric surface and an explicit rule that turns that figure-level support into the current runtime value and uncertainty.
- `BERUT_2012_SOURCE_SURFACE_NOTE.md` and `berut_2012_source_surface_note.json` now narrow the Berut blocker again, but they do not close it: the currently visible Nature page surface still looks like a figure-level preview rather than an exposed numeric table, so stronger closure still requires either numeric-point capture within the selected preview locator or a stronger upstream numeric surface.
- `BERUT_2012_TRANSCRIPTION_POLICY_BLOCKER.md`, `berut_2012_transcription_policy_blocker.json`, `BERUT_2012_TRANSCRIPTION_POLICY_DECISION.md`, and `berut_2012_transcription_policy_decision.json` now narrow the Berut blocker one step further, but they do not close it: the repo selects `figure_level_locator_capture` as the conservative path and now attaches `Figure 3` as the current preview-level locator, yet it still lacks one numeric point/curve capture or stronger upstream surface and the final mapping rule into the runtime summary row.
- `JUN_2014_UNCERTAINTY_GAP.md` and `jun_2014_uncertainty_gap.json` now make the `Jun` blocker more explicit, but they do not close it: the topic now carries a first-pass interval for the pinned Jun asymptotic-work summary and the legacy `0.028 eV` row is demoted out of active Jun logic, yet it still lacks the final-source parity/local archival for the captured source-summary locator needed to treat that interval as a clean source-normalized Jun row.
- `JUN_2014_RUNTIME_MAPPING_CONFLICT.md` and `jun_2014_runtime_mapping_conflict.json` now make a second `Jun` blocker more explicit, but they do not close it: the pinned `Jun 2014` asymptotic-work summary (`0.71 +/- 0.03 kT`) converts to only about `0.01836 +/- 0.00078 eV` under the current `300 K` verifier baseline, so the legacy `0.028 eV` runtime row is now demoted out of active Jun logic; the remaining Jun blocker is the original final-source parity/local archival for the captured Table 1/Figure 4 fit target, not just a missing conversion note.
- `HONG_2016_SOURCE_LINEAGE_NOTE.md` and `hong_2016_source_lineage_note.json` now make a third blocker more explicit, but they do not close it: the legacy `0.028 eV / 44% above limit` runtime wording may reflect a later nanomagnetic-memory experiment branch, and an accessible same-author preprint now exposes Hong-side numeric candidates, so the row is not yet trustworthy as a clean `Jun 2014` benchmark quantity and should not be reassigned to `Hong 2016` until the provisionally preferred target is confirmed at the final-source layer.
- `docs/data/external/thermodynamics/landauer/hong_2016/source_record.json` now stages a candidate alternate branch, carries a confirmed Crossref DOI metadata anchor (`DOI 10.1126/sciadv.1501492`, `PMID 26998519`, `PMCID PMC4795654`), and records an accessible same-author preprint precursor surface with source-facing dissipation values, but it still does not close the blocker: the repo does not yet archive one direct final official article page or confirm that the provisionally preferred `4.2 +/- 0.9 zJ` target is the final-source comparison quantity.
- The locally archived Crossref snapshot improves bibliographic provenance, but it is still metadata-only and must not be mistaken for a row-level source capture or a direct publisher-page archive.
- `HONG_2016_NUMERIC_MISMATCH_NOTE.md`, `hong_2016_numeric_mismatch_note.json`, `HONG_2016_RUNTIME_TARGET_POLICY.md`, and `hong_2016_runtime_target_policy.json` now make a fourth blocker explicit, but they do not close it: current topic policy provisionally prefers the preprint temperature-series mean near `~0.0262 eV` because it best fits the inherited `44% above limit` / `~0.026 eV` narrative, while the local runtime row still uses `0.028 eV` and the final-source confirmation is still missing.
- `HONG_2016_SOURCE_ACQUISITION_BLOCKER.md` and `hong_2016_source_acquisition_blocker.json` now make a fifth blocker explicit, but they do not close it: the likely Hong paper identity is visible from secondary sources and a same-author preprint precursor is accessible, yet the repo still lacks a final archived official article page for that branch.
- `PETERSON_2018_SOURCE_CONFLICT.md` and `peterson_2018_source_conflict.json` now make the `Peterson` blocker more explicit, but they do not close it: direct metadata checks now show that the local runtime branch mixes a Nature Physics mesoscopic-entropy DOI with a trapped-ion quantum-Landauer narrative from a different 2018 PRL source, so the branch cannot enter row capture or gate logic yet.
- `PETERSON_BRANCH_IDENTITY_POLICY.md` and `peterson_branch_identity_policy.json` now narrow the Peterson blocker again, but they do not close it: current evidence separates the Peterson-led 2016 authorship cue from the trapped-ion PRL 2018 narrative and from the Nature Physics 2018 DOI, so the local `Peterson 2018` label is now treated as a demoted legacy placeholder rather than an active benchmark identity.
- `MEASURED_CONSTANT_UNCERTAINTY_PACKAGE.md` and `measured_constant_uncertainty_package.json` now make the measured-constant layer more explicit, but they do not close it: the package threads a provisional `G` proxy into gravity-context combined intervals, yet those intervals still rely on a local proxy rather than direct 2022 extraction and still exclude spin/systematic terms.
- `DERIVATION_MAP.md` and `bridge_derivation_map.json` now make the proof boundary explicit, but they do not close it: the topic still lacks a dimensional UET bridge contract and a non-circular mapping from UET variables to imported Landauer/gravity identities.
- `UNITS_CONTRACT.md` and `units_contract.json` now separate SI and proxy layers, but they currently stop at a boundary: they do not yet justify any conversion from engine proxy variables into physical thermodynamic observables.
- `LANDAUER_UET_MAPPING.md` and `landauer_uet_mapping.json` now make the Landauer lane more honest: the current engine path preserves the imported lower bound, but it still does not demonstrate a nontrivial UET-added mapping in that lane.
- `BETA_ROLE.md` and `beta_role_clarification.json` now make `beta` usage more honest: `beta` appears in topic language and code, but current evidence still does not close it as a derived thermodynamic bridge coefficient.
- `thermodynamic_bridge_foundation_claim_gate.json` is a claim-inheritance control. It can allow dependent topics to inherit lower-bound/formula-consistency lanes, but it cannot upgrade the UET-specific bridge while source and derivation blockers remain open.
- `thermodynamic_claim_scope_gate` is the artifact export controller. It may allow lower-bound and standard-formula language, but it must block UET bridge proof, exact-bridge, source-normalized validation, and Tier-A-complete wording while the current blockers remain open.
- `Research_Landauer.py` verifies exact-constant consistency and lower-bound behavior; it does not prove the complete UET bridge mechanism.
- Bekenstein, Unruh, Hawking, and Josephson formulas are established physics/metrology identities. They constrain the bridge but do not independently validate UET dynamics.
- The Cattaneo benchmark is synthetic and fitted; it is useful for model-shape checking but cannot be cited as external experimental evidence.
- The vacuum entropy-sink script is an open hypothesis sandbox. It requires conservation-law accounting and an independently motivated physical mechanism before it can support core theory claims.
- Internal script execution does not by itself establish external replication, theorem-level proof, or broad physical closure.

## Current Claim Boundary

| Claim area | Allowed wording now | Blocker to stronger wording |
|:--|:--|:--|
| Landauer bridge | Source-record-backed lower-bound consistency check with partial interval support | Berut stronger-surface-or-policy closure, Jun legacy-row split/replace/relabel plus uncertainty row, Hong final-source confirmation for the provisionally preferred target plus a keep/replace/remove decision for the legacy row, and dynamic UET prediction beyond the lower bound. |
| Thermodynamic gravity links | Consistency with standard formulas | Formal derivation showing how UET field variables produce the relation, not only reuse it. |
| Non-equilibrium heat transport | Synthetic Cattaneo-style lag demonstration | Real dataset or declared simulation-only role with fixed parameters. |
| Vacuum entropy sink | Hypothesis sandbox | Physical mechanism, conservation accounting, and falsifiable test. |
| Provenance workflow | Intake/readiness gate exists for missing source packages | Filled evidence entries, archived upstream files, and source-review closure. |
| Dependent theory topics | May inherit lower-bound and standard-formula constraints from the foundation claim gate | UET bridge proof, source-normalized dataset closure, uncertainty propagation, and dependency proof. |

