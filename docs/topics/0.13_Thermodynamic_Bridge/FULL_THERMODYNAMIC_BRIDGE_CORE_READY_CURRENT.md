# Topic 0.13 Current Full-Bridge State

This is the current human-readable companion to:

`Result/artifacts/topic13_full_thermodynamic_bridge_core_ready_gate.json`

The earlier `FULL_THERMODYNAMIC_BRIDGE_CORE_READY.md` records the initial
wave. This note records the later scoped no-go narrowing and must not be read
as a Core-ready promotion.

MAJOR_RESULT_CLOSURE: `PARTIAL`

WHAT_IS_ACTUALLY_CLOSED: The normalized TTG and Phi operators, the frozen-C
compact-support control, the constraint-only Landauer/standard identities, and
the scoped structural no-go assessment for the declared conserved-C
local-gradient Cattaneo class are artifact-linked.

WHAT_REMAINS_OPEN: A named finite-cone branch or explicit conserved-C
regularization, permitted source-normalized TTG rows, independent
`alpha_Phi_K`, the non-circular UET bridge and `beta`, charge EOS, covariant
transport, SK/KMS matching, entropy-current/dissipative balance, and the
dimensional heat/entropy maps.

DEPENDENCY_UNLOCKED: No downstream Core curved 3+1, Gravity/GR,
constitutive-transport, or Galaxy dependency is unlocked. Only normalized and
internal thermal-control inheritance remains available.

STATUS: `BLOCKED_OPEN_T13_FULL_BRIDGE`

WHAT_CHANGED: The conserved-C structural issue is now recorded as a scoped
no-go result. The result is not silently substituted with a new equation, and
the original full-candidate leakage failure remains preserved.

EQUATION_OR_MAPPING:

```text
y_TTG = Delta_Tq(t) / Delta_Tq(0)
y_TTG^UET = Delta_Phi(t) / Delta_Phi(0)
Delta_Tq = alpha_Phi_K * Delta_Phi
```

VERIFICATION: The selected frozen-C control has zero measured pre-arrival
leakage, while the full candidate remains above the locked `1e-6` threshold.
The no-go assessment is limited to the declared local conserved-C
gradient-energy class; it is not a theorem for all regularizations or
nonlocal laws. Xie 2026 remains metadata-only and the holdout-integrity gate
passes without calibration access.

CONTROLLING_BLOCKER: `named_finite_cone_branch_or_explicit_regularization_missing`,
`ttg_numeric_source_package_is_provisional`,
`alpha_Phi_K_independent_calibration_missing`,
`non_circular_uet_bridge_and_beta_derivation_missing`,
`eos_transport_kms_entropy_completion_missing`, and
`dimensional_phi_to_thermal_observable_map_missing`.

NEXT_ACTION: Define and verify a named finite-cone branch or explicit
conserved-C regularization without clipping, cone padding, threshold changes,
parameter fitting, or ontology changes. Then close source provenance and
independent calibration before promoting the bridge, EOS, transport, KMS, or
entropy results.

CLAIM_BOUNDARY: `C` remains a collective system-behaviour coordinate, `Phi`
remains an effective response variable, and `R_gen` remains a derived history
trace without backreaction. No temperature prediction, external validation,
global UET closure, or downstream dependency unlock is claimed.
