# Daily Progress Report - 2026-03-20

## Summary

Today's work focused on making the repository safer to commit again. The main themes were Git cleanup, `uet_web` tracking repair, ignore rule cleanup, and documentation path stabilization.

**Important status:** no new commit has been made for this cleanup session yet, and no push has happened yet.

## Completed Today

### 1. Git structure cleanup

- Investigated why commit/push preparation had become difficult.
- Confirmed that `uet_web` had been stuck in a bad Git state as a gitlink/submodule-like entry in the parent repo.
- Backed up the old nested Git metadata for `uet_web` instead of deleting it blindly.
- Recovered `uet_web` as a normal tracked directory in the parent repository.
- Ran repository cleanup to reduce Git housekeeping issues.

### 2. File audit before commit

- Audited staged, unstaged, and untracked files.
- Separated system-relevant files from local/generated/archive noise.
- Identified that `uet_knowledge_state.json` is generated semantic/knowledge state and should not be committed.
- Confirmed Rust build output like `target/` should stay ignored.

### 3. Ignore rule cleanup

Updated root `.gitignore` to reduce repository bloat and accidental commits of local-only files.

Key ignored items now include:

- `/uet_history/`
- `/uet_knowledge_state.json`
- `.obsidian/`
- `target/`
- `uet_core/target/`

Also adjusted the `uet_web/Docs_software_design` behavior so a dedicated reporting folder can be tracked while the rest can remain local-only by default.

### 4. Documentation path stabilization

Found a docs path inconsistency in the web app:

- local docs currently exist under `uet_web/Docs_software_design/...`
- some old Git/index references still pointed to `uet_web/docs/...`
- the chat API was still relying on the older platform specs path

Updated `uet_web/src/app/api/chat/route.ts` so it can resolve platform specs from the current `Docs_software_design/platform_specs` path first, with the old path as fallback.

### 5. Reporting standard created

Created a reporting location for daily pre-commit reporting:

- `uet_web/Docs_software_design/daily_reports/README.md`

This folder is intended to become the standard place for daily progress reports before commits.

## Current Repository State

Current Git status at time of writing:

- branch: `main`
- branch relation: `ahead 32, behind 33`
- latest visible local commit: `09cea6bde`
- **current cleanup work is not committed yet**
- **nothing from this cleanup/reporting round has been pushed yet**

There are still many repository changes present, including:

- root config changes:
  - `.gitignore`
  - `Dockerfile.web`
  - `docker-compose.yml`
- backend/runtime changes:
  - `uet_agents/base_agent.py`
  - `uet_api/src/main.rs`
  - `uet_core/src/calculator.rs`
  - new untracked agent files under `uet_agents/`
  - new untracked `uet_api/src/agent.rs`
- large `uet_web` changes caused by the earlier recovery from gitlink to normal directory tracking
- modified `uet_web/src/app/api/chat/route.ts`

## Risks / Open Issues

### 1. Branch divergence remains

The local branch is still both ahead of and behind `origin/main`. That means GitHub sync is not solved yet. After the next local commit, pull/rebase/merge strategy still needs to be handled carefully.

### 2. `uet_web` commit scope is still large

Because `uet_web` had to be repaired from bad Git tracking, Git currently sees a large amount of content there. The next commit must be reviewed carefully so that only intended work is included.

### 3. Documentation layout is not fully standardized yet

The repository now has a daily reporting structure, but the broader docs layout still needs a clean final decision so that there is one clear long-term standard.

## Recommended Next Actions

1. Review the final intended commit scope.
2. Keep local-only docs/history/generated files out of the commit.
3. Write or update the daily report before each commit.
4. Create the next commit only after the scope is confirmed.
5. Handle branch divergence before pushing to GitHub.

## Pre-Commit Decision

**Decision right now:** do not commit yet.

Reason:

- the repo is cleaner than before,
- the reporting standard is now being established,
- but the final commit scope still needs one more careful pass before creating the next commit.
