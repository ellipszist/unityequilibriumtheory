# Work Ledger - 2026-07-26

## Foundation-first equation research workflow repair

- Area: `research-standards` (secondary: `research-core`)
- Scope: repository standards, `docs/core/` equation workflow, registry, dependency gate, and audit tooling
- Work: Added the canonical foundation-first F0-F8 equation research standard, folder-scoped core instructions, an initial core equation correspondence registry, a machine-readable protocol, and a blocked foundation dependency gate. Narrowed the legacy core README boundary and linked the new protocol from the existing standards workflow.
- Verification: `.venv\Scripts\python.exe docs/scripts/audit/audit_uet_equation_foundation.py --json`; audit schema/path result `PASS`, foundation gate result `BLOCKED` with controller `foundation_inventory_and_correspondence_registry_incomplete`; `git diff --check` returned no whitespace errors.
- Public-safety: `blocked` for physical promotion; documentation and workflow repair are safe, while the registry is explicitly marked `INITIAL_SEED_NOT_EXHAUSTIVE`.
- Files/artifacts: `AGENTS.md`, `docs/core/AGENTS.md`, `docs/core/README.md`, `docs/core/artifacts/uet_equation_correspondence_registry.json`, `docs/core/artifacts/uet_equation_research_protocol.json`, `docs/core/artifacts/uet_foundation_dependency_gate.json`, `docs/scripts/audit/audit_uet_equation_foundation.py`, and linked `For Work` standards.
- Remaining: complete the F0 inventory across numbered topic equations, build the full UET-to-standard-physics correspondence matrix, and keep all downstream claims at the current blocked/diagnostic ceiling.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.

## Foundation compatibility and limiting-case audit

- Area: `research-core` (secondary: `research-standards`)
- Scope: declared legacy equations, implementation correspondence, units, standard-theory limits, and foundation principles
- Work: Added a generated compatibility gate, an audit regression test, and `UET_FOUNDATION_COMPATIBILITY_AUDIT.md`. The audit separates implementation contradictions from unresolved physical correspondence and conditional limiting cases.
- Verification: `.venv\Scripts\python.exe docs/scripts/audit/audit_uet_foundation_compatibility.py`; audit execution `PASS`, compatibility gate `BLOCKED`; 15 findings include the legacy potential/derivative contradiction, legacy information-operator conflict, beta/Landauer unit conflict, matter-space causal leakage blocker, and rejected O(2)-to-double-well reduction.
- Public-safety: `blocked` for physical promotion; GR closed limit and O(2) EOS remain `COMPATIBLE_CONDITIONAL` only.
- Files/artifacts: `docs/scripts/audit/audit_uet_foundation_compatibility.py`, `docs/core/artifacts/uet_foundation_compatibility_gate.json`, `docs/core/UET_FOUNDATION_COMPATIBILITY_AUDIT.md`, and `docs/core/test/test_foundation_compatibility.py`.
- Remaining: repair the legacy conflicts, complete equation inventory/correspondence, and repair matter-space pre-arrival leakage before any downstream real-data claim.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## F0 equation inventory and cross-topic correspondence matrix

- Area: `research-standards` (secondary: `research-core`)
- Scope: all numbered-topic `FORMULA_AUDIT.md` registries and selected foundation/dependency rows
- Work: Added generated F0 inventory for 27 formula-audit files and 260 rows, plus a 19-row critical correspondence matrix separating standard counterpart, UET derivation status, and special-case status.
- Verification: `build_uet_equation_inventory.py` returned `PASS_WITH_DISCLOSED_GAPS` with inventory gate `BLOCKED`; no duplicate formula IDs; inventory and matrix regression suite passed 5 tests.
- Public-safety: `blocked` for foundation promotion; topic 0.1 remains scaffold-blocked, standard formulas remain baselines, and UET bridges remain open unless separately derived.
- Files/artifacts: `docs/scripts/audit/build_uet_equation_inventory.py`, `docs/core/artifacts/uet_foundation_equation_inventory.json`, `docs/scripts/audit/build_uet_foundation_correspondence_matrix.py`, `docs/core/artifacts/uet_foundation_correspondence_matrix.json`, `docs/core/UET_FOUNDATION_EQUATION_INVENTORY.md`, `docs/core/UET_FOUNDATION_CORRESPONDENCE_MATRIX.md`, and related tests.
- Remaining: complete code-only formula inventory, close correspondence/observable maps, and audit selected open rows with equation residuals and unit contracts.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Special-case reproducibility repair

- Area: `research-core`
- Scope: O2 finite-density EOS, covariant superfluid transport, GR program gate, and limiting-case regression suite
- Work: Regenerated the five Wave-10 artifacts through `audit_uet_o2_superfluid_transport.py` after detecting inconsistent `generated_at` provenance in the stored program gate. No physics status was upgraded.
- Verification: O2/GR/matter-space special-case suite now passes `36 tests`; GR closed-limit verifier remains `PASS` algebraic/local, O2 program remains `BLOCKED`, and matter-space verifier remains `FAIL` at `prearrival_leakage`.
- Public-safety: `blocked` for physical promotion; this wave repairs reproducibility metadata only.
- Files/artifacts: generated O2/transport/program artifacts and refreshed compatibility/matrix inputs.
- Remaining: preserve generated-at/source snapshot consistency in future artifact waves and continue unresolved correspondence/causal gates.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Code-only equation surface inventory

- Area: `research-core` (secondary: `research-standards`)
- Scope: 32 core implementation/proof Python modules, excluding data/test/scripts support trees
- Work: Added a conservative static inventory of 1,450 equation-like code surfaces and 25 core files without registry path links. Every candidate is explicitly `REVIEW_REQUIRED`, ontology/unit/derivation `OPEN`, and `NOT_EVIDENCE`.
- Verification: `build_uet_code_surface_inventory.py` returned `PASS_WITH_DISCLOSED_GAPS` with inventory gate `BLOCKED`; static scan is not treated as equation proof.
- Public-safety: `blocked` for foundation promotion; the scan prevents omission but does not classify physical meaning automatically.
- Files/artifacts: `docs/scripts/audit/build_uet_code_surface_inventory.py`, `docs/core/artifacts/uet_code_surface_inventory.json`, `docs/core/UET_CODE_SURFACE_INVENTORY.md`, `docs/core/test/test_uet_code_surface_inventory.py`, and core reading rules.
- Remaining: classify candidate surfaces into core equation, constitutive/application, standard baseline, numerical detail, or legacy/quarantine; then close F1-F3 for the true core subset.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Core equation-family ownership contract

- Area: `research-core` (secondary: `research-standards`)
- Scope: 32 core implementation modules and their equation-family/support ownership
- Work: Added a generated F1-F3 family contract assigning all 32 modules to 9 equation families plus 3 support/contract groups. Each family now declares ontology variables, standard counterpart, unit lane, derivation class, mathematical status, special-case status, and claim ceiling.
- Verification: `build_uet_core_equation_family_contract.py` returned `PASS` structurally with `32/32` module assignment and contract status `BLOCKED` from upstream gates; family-contract regression tests added.
- Public-safety: `blocked` for theory promotion; assignment closes ownership ambiguity but does not prove any family physically.
- Files/artifacts: `docs/scripts/audit/build_uet_core_equation_family_contract.py`, `docs/core/artifacts/uet_core_equation_family_contract.json`, `docs/core/UET_CORE_EQUATION_FAMILY_CONTRACT.md`, and related tests/core instructions.
- Remaining: close F1-F3 for the equation families with open lanes, then perform family-specific formal/numerical residual checks and observable mapping.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Foundation status aggregate and stopping boundary

- Area: `research-standards` (secondary: `research-core`)
- Scope: cross-family F0-F8 status, conditional special cases, and foundation stopping criteria
- Work: Added a generated aggregate artifact and report that combine inventory, correspondence, compatibility, family ownership, matter-space, GR, O2, and trace evidence. Linked the aggregate into the core instructions and README.
- Verification: aggregate `audit_status=PASS`, `foundation_status=BLOCKED`, with `8 BLOCKED` and `1 PASS_CONDITIONAL` gates; targeted foundation/special-case suite passed `48 tests`; all aggregate input audits regenerated successfully.
- Public-safety: `blocked` for physical promotion; conditional GR/O2/comparator limits remain narrow and do not establish a universal UET theory.
- Files/artifacts: `docs/scripts/audit/build_uet_foundation_status_aggregate.py`, `docs/core/artifacts/uet_foundation_status_aggregate.json`, `docs/core/UET_FOUNDATION_STATUS_AGGREGATE.md`, `docs/core/test/test_foundation_status_aggregate.py`, plus core workflow links.
- Remaining: close family-specific F1-F7 blockers in dependency order; do not start downstream physical claims while contradictions, unit/correspondence gaps, or causal leakage remain.
- Commit status: this aggregate wave is ready for scoped local commit; unrelated worktree changes remain unstaged.

## Legacy variational closure audit

- Area: `research-core` (secondary: `research-standards`)
- Scope: exact functional/gradient consistency of the legacy `C` potential and `I` coupling
- Work: Added a generated audit that evaluates the coded potential derivative by finite difference and checks the information-source sign implied by the declared `+beta*C*I` coupling.
- Verification: audit `audit_status=PASS`, `closure_status=BLOCKED`; potential derivative residual `13.675000000367197` against `1e-8`; information source expected sign `-1` but coded sign `+1`; aggregate now reports 9 controlling blockers; legacy/core regression suite remains the controlling evidence.
- Public-safety: `blocked`; legacy engine remains unchanged and quarantined as comparator.
- Files/artifacts: `docs/scripts/audit/audit_uet_legacy_variational_closure.py`, `docs/core/artifacts/uet_legacy_variational_closure.json`, `docs/core/test/test_legacy_variational_closure.py`, and aggregate/core links.
- Remaining: repair or explicitly relabel the legacy functional/gradient pair before using it as the foundation for a standard-theory limit.
- Commit status: ready for a scoped local commit; unrelated worktree changes remain unstaged.
## Special-case limit and provenance revalidation

- Area: `research-core` (secondary: `research-standards`)
- Scope: matter-space, thermal/phase pilots, covariant GR/O2 limits, and dependency hash chains
- Work: Regenerated core and pilot artifacts from their source verifiers after the legacy closure audit; refreshed thermal/phase pilot metadata and report-alignment hashes so the dependency chain is reproducible.
- Verification: special-case/core regression suite passed `111 tests`; program gate remains `BLOCKED` by `core_prearrival_leakage`, with `16/17` core gates passing, pilot artifact integrity `PASS`, and layout `WARN`.
- Public-safety: `blocked` for physical promotion; GR/O2 remain conditional lanes, thermal remains `SIMULATION_ONLY`, phase remains `INTERNAL_DIAGNOSTIC`.
- Files/artifacts: regenerated core/special-case/pilot artifacts and `docs/core/artifacts/matter_space_report_alignment_gate.json`.
- Remaining: causal response repair and physical observable/unit maps; no standard-theory or real-data claim is promoted by this revalidation.
- Commit status: ready for a scoped local commit; unrelated worktree changes remain unstaged.